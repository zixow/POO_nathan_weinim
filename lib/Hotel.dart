import 'package:flutter/material.dart';

class PageHotels extends StatelessWidget {
  const PageHotels({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Page des hôtels", style: TextStyle(fontSize: 22)),
    );
  }
}